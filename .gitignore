# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Environments
.venv/
venv/
env/

# Secrets — .env.example is committed, .env is not
.env

# Generated reports (including --output-dir variants and lock fallbacks)
output/
output_*/
triage_report-*.csv

# Run logs
logs/
*.log

# Live intel cache (KEV catalogue, EPSS scores, NVD records)
.cache/

# CrewAI local artifacts
.crewai/
db/
*.db

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Personal notes, not portfolio material
*.docx

# Real scanner exports. `--source csv` reads these, and they carry live estate
# detail -- hostnames, addresses, OS versions, open ports, unpatched plugins.
# The sample export under data/ is the only findings file this repo commits.
*-Scan*.csv
scans/
